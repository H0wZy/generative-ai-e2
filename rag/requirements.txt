# RAG dependencies — mirror of [project].dependencies in rag/pyproject.toml.
# torch is NOT listed here on purpose: sentence-transformers pulls it, and the
# default PyPI wheel ships CUDA (~3.9 GB). `make install-rag` installs the
# CPU-only build first, which is enough for all-MiniLM-L6-v2.
sqlite-vec>=0.1.6
sentence-transformers>=3.0,<4.0
fastmcp>=2.0,<3.0
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
pytest>=8.0,<10.0
pytest-asyncio>=0.24,<1.0
